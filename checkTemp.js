function checkTemp(temp) {
    console.log("Temp on "+temp)

if ( temp >= 21 && temp <= 40 ) {
    return 0;

} if ( temp > 0 && temp <= 20 ) {
    return -1;

} else if ( temp > 40 && temp < 60) 
    return 1;


}
